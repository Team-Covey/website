{
  "name": "teamcovey-website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --port 4321 --persist-to node_modules/.cache/wrangler",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
